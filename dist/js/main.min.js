"use strict";

var vacanciContainers = document.querySelectorAll('.vacanci-container');
var tabBtns = document.querySelectorAll('.tab-btn');
tabBtns.forEach(function (button) {
  button.addEventListener('click', function () {
    tabBtns.forEach(function (button) {
      button.classList.remove('active');
    });
    button.classList.add('active');
  });
});
function toggleVacanci(n) {
  vacanciContainers.forEach(function (item) {
    var itemAttr = item.getAttribute('vacanci-data');
    if (n == Number(itemAttr)) {
      vacanciContainers.forEach(function (item) {
        item.classList.remove('active');
      });
      item.classList.add('active');
    }
  });
}
var header = document.querySelector('.header-wrapper');
window.addEventListener('scroll', function () {
  if (window.scrollY >= 10) {
    header.style.backgroundColor = '#fff';
  } else {
    header.style.background = 'linear-gradient(90deg, rgba(255, 255, 255, 0.30) 0%, rgba(255, 255, 255, 0.60) 100%)';
  }
});
var uploadFile = document.getElementById('uploadFile');
var uploadFileNames = document.querySelector('.upload-file-names');
uploadFile.addEventListener('change', function () {
  if (uploadFile.files && uploadFile.files.length > 0) {
    Array.from(uploadFile.files).forEach(function (file) {
      var fileName = document.createElement('div');
      fileName.classList.add('file');
      fileName.innerHTML = "\n            <span class=\"file-name\">".concat(file.name, "</span>\n            <img src=\"images/cancel-file.svg\" alt=\"cancel\" width=\"10\" height=\"10\" id=\"remove-file\">\n        ");
      uploadFileNames === null || uploadFileNames === void 0 ? void 0 : uploadFileNames.insertBefore(fileName, uploadFileNames.firstChild);
      var cancelBtn = fileName.querySelector('#remove-file');
      cancelBtn.addEventListener('click', function () {
        fileName.remove();
      });
    });
  }
});
//# sourceMappingURL=main.min.js.map
